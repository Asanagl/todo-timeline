name: Lock Threads

on:
  schedule:
    - cron: '0 0 * * *'

permissions:
  issues: write
  pull-requests: write

jobs:
  lock:
    runs-on: ubuntu-latest
    steps:
      - uses: dessant/lock-threads@v4
        with:
          issue-inactive-days: '30'
          issue-comment: >
            This issue has been automatically locked since there has not been
            any recent activity after it was closed. Please open a new issue
            for related bugs.
          issue-lock-reason: 'resolved'
          pr-inactive-days: '30'
          pr-comment: >
            This pull request has been automatically locked since there has not been
            any recent activity after it was merged. Please open a new issue
            for related bugs.
          pr-lock-reason: 'resolved'
